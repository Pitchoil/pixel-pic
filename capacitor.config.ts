import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pixelpic.sticker',
  appName: 'Pixel-Pic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
